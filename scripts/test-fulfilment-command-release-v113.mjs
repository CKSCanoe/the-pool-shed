import fs from 'node:fs';import assert from 'node:assert/strict';import pkg from '../package.json' with {type:'json'};
const release=pkg.version,html=fs.readFileSync('public/index.html','utf8'),sw=fs.readFileSync('public/service-worker.js','utf8');const [major,minor]=release.split('.').map(Number);assert(major>1||(major===1&&minor>=13),'Fulfilment regression guard requires release 1.13.0 or newer');for(const asset of [`./fulfilment-control-engine.js?v=${release}`,`./fulfilment-workspace.js?v=${release}`,`./assets/css/app.css?v=${release}`])assert(html.includes(asset),`Runtime missing ${asset}`);assert(html.indexOf(`./fulfilment-control-engine.js?v=${release}`)<html.indexOf(`./fulfilment-workspace.js?v=${release}`));assert(sw.includes(`pool-shed-v${release}-`));for(const asset of [`./fulfilment-control-engine.js?v=${release}`,`./fulfilment-workspace.js?v=${release}`])assert(sw.includes(asset));console.log(`PASS Fulfilment Command retained on v${release}`);
