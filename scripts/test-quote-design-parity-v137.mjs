import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url));
const root=path.resolve(here,'..');
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const pkg=JSON.parse(read('package.json'));
const ws=read('public/quote-studio-workspace.js');
const css=read('public/quote-studio.css');
const eng=read('public/quote-studio-engine.js');
const api=read('api/quote.js');
const server=read('server/quote.js');
assert.equal(pkg.version,'1.45.1');
assert.match(ws,/builderPanel='elements'/,'Design Lab should open on Elements');
assert.match(ws,/Elements<\/button>.*Products<\/button>.*Sections<\/button>.*Uploads<\/button>/s,'Left library order must match Design Lab interaction model');
assert.match(ws,/toggle-builder-left/);assert.match(ws,/toggle-builder-right/);assert.match(ws,/focus-canvas/);
assert.match(ws,/canvas-desktop/);assert.match(ws,/canvas-tablet/);assert.match(ws,/canvas-mobile/);assert.match(ws,/zoom-out/);assert.match(ws,/zoom-in/);
assert.match(css,/\.qs-builder\.elite\.focus-canvas/,'Focus canvas layout must exist');
assert.match(css,/\.qs-studio-root\.qs-build-mode/,'Build Quote must use a dedicated compact design workspace');
assert.match(css,/\.qs-canvas-stage\.device-tablet/);assert.match(css,/\.qs-canvas-stage\.device-mobile/);
assert.match(css,/\.qs-artboard-shell/);assert.match(css,/LIVE CLIENT CANVAS|qs-canvas-toolbar-left/);
// Automation authority must remain intact through the visual rebuild.
assert.match(api,/customerConfirmation:contact/);
assert.match(server,/confirmedCustomer\(acceptance\)/);
assert.match(eng,/createSalesOrderOnAcceptance:true/);
assert.match(eng,/allocateStockOnAcceptance:true/);
assert.match(eng,/createDraftPurchaseOrders:true/);
console.log('Pool Shed v1.38 Design Lab parity + automation authority: PASS');
