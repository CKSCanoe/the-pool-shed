import fs from 'node:fs';import assert from 'node:assert/strict';import pkg from '../package.json' with {type:'json'};
const release=pkg.version,html=fs.readFileSync('public/index.html','utf8'),sw=fs.readFileSync('public/service-worker.js','utf8');const [major,minor]=release.split('.').map(Number);assert(major>1||(major===1&&minor>=16),'Finance Command regression guard requires release 1.16.0 or newer');
assert(html.includes(`./assets/css/app.css?v=${release}`),'index must load current app.css');for(const asset of ['finance-command-engine.js','finance-command-workspace.js']){assert(html.includes(asset+`?v=${release}`),`index must load ${asset} at current release`);assert(sw.includes('./'+asset+`?v=${release}`),`service worker must cache ${asset}`);}assert(!html.includes('finance-command.css'),'Finance Command CSS must be compiled into app.css, not loaded separately');assert(fs.readFileSync('scripts/build-css.mjs','utf8').includes('system/50-finance-command.css'),'CSS build must retain Finance Command authority module');assert(html.indexOf('finance-command-engine.js')<html.indexOf('finance-command-workspace.js'),'Finance Command engine must load before workspace');assert(sw.includes(`pool-shed-v${release}-`),'service worker cache must use current release version');console.log(`PASS Finance Command runtime, cache and visual authority retained on v${release}`);
