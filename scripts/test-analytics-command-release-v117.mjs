import fs from 'node:fs';import assert from 'node:assert/strict';import pkg from '../package.json' with {type:'json'};
const release=pkg.version,html=fs.readFileSync('public/index.html','utf8'),sw=fs.readFileSync('public/service-worker.js','utf8'),build=fs.readFileSync('scripts/build-css.mjs','utf8');const [major,minor]=release.split('.').map(Number);assert(major>1||(major===1&&minor>=17),'Analytics Command regression guard requires release 1.17.0 or newer');assert(html.includes(`./assets/css/app.css?v=${release}`));for(const asset of ['analytics-command-engine.js','analytics-command-workspace.js']){assert(html.includes(asset+`?v=${release}`),`index missing ${asset}`);assert(sw.includes('./'+asset+`?v=${release}`),`SW missing ${asset}`);}assert(html.indexOf('analytics-command-engine.js')<html.indexOf('analytics-command-workspace.js'));assert(build.includes('system/51-analytics-command.css'));assert(sw.includes(`pool-shed-v${release}-`),'service-worker cache must use current release');console.log(`PASS Analytics Command runtime, cache and visual authority retained on v${release}`);
