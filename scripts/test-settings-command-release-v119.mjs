import fs from 'node:fs';import assert from 'node:assert/strict';import pkg from '../package.json' with {type:'json'};
const release=pkg.version,html=fs.readFileSync('public/index.html','utf8'),sw=fs.readFileSync('public/service-worker.js','utf8'),build=fs.readFileSync('scripts/build-css.mjs','utf8'),legacy=fs.readFileSync('public/assets/js/01-legacy-01.js','utf8');assert(Number(release.split('.')[1])>=19,'Settings Command requires release 1.19.0 or newer');for(const f of ['settings-permissions-engine.js','settings-command-workspace.js']){assert(html.includes(`./${f}?v=${release}`),`index missing ${f}`);assert(sw.includes(`./${f}?v=${release}`),`service worker missing ${f}`);}assert(html.indexOf('settings-permissions-engine.js')<html.indexOf('assistant-engine.js'),'permission engine must load before assistant');assert(html.indexOf('automation-command-workspace.js')<html.indexOf('settings-command-workspace.js'),'Settings workspace must load after Automation settings provider');assert(build.includes('system/53-settings-command.css'),'CSS build must include Settings Command');assert(legacy.includes('PoolShedSettingsPermissions'),'legacy tab access must delegate to Settings permission authority');assert(legacy.includes(`service-worker.js?v=${release}`));console.log('PASS Settings Command runtime, permission ordering, CSS and current cache wiring');
