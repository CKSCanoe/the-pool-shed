import fs from 'node:fs';import assert from 'node:assert/strict';
const finance=fs.readFileSync('public/finance-command-workspace.js','utf8');
const legacy=fs.readFileSync('public/accounting-workspace.js','utf8');
const settings=fs.readFileSync('public/settings-command-workspace.js','utf8');
assert(finance.includes('Ready to Connect'),'Finance Command must visibly distinguish Xero readiness from a live connection');
assert(finance.includes('liveEnabled'),'Finance Command must gate connection/sync controls from server readiness');
assert(legacy.includes('Ready to Connect'),'Legacy Accounting & Xero surface must also respect the staging gate');
assert(settings.includes('Xero Connection Readiness'),'Settings Integrations must expose Xero readiness rather than a generic connection modal');
assert(settings.includes('Live connection locked'),'Settings must make the deliberate hold obvious');
console.log('PASS Xero readiness UI contract');
