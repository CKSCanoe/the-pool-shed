import fs from 'node:fs';import assert from 'node:assert/strict';import pkg from '../package.json' with {type:'json'};
const release=pkg.version,html=fs.readFileSync('public/index.html','utf8'),sw=fs.readFileSync('public/service-worker.js','utf8'),legacy=fs.readFileSync('public/assets/js/01-legacy-01.js','utf8'),build=fs.readFileSync('scripts/build-css.mjs','utf8');const [major,minor]=release.split('.').map(Number);assert(major>1||(major===1&&minor>=18),'Automation regression guard requires release 1.18.0 or newer');for(const f of ['assistant-engine.js','automation-command-engine.js','automation-command-workspace.js']){assert(html.includes(`./${f}?v=${release}`),`missing ${f} at current release`);assert(sw.includes(`./${f}?v=${release}`),`service worker missing ${f}`);}assert(html.includes(`app.css?v=${release}`));assert(sw.includes(`pool-shed-v${release}-`));assert(legacy.includes('{ id: "automation", label: "Automation"'),'Automation must be first-class navigation');assert(legacy.includes('__POOL_SHED_CAN_ACCESS__'),'permission bridge missing');assert(build.includes('system/52-automation-command.css'));console.log(`PASS Automation + Assistant retained on v${release}`);
