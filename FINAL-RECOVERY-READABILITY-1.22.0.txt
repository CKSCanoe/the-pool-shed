POOL SHED v1.22.0 - RECOVERY & READABILITY

Release purpose:
- Fix old Pool Shed records not reaching the new canonical system.
- Fix bright aqua legacy CSS overriding readable neutral table styling.
- Prevent stale cached CSS/JS from surviving a corrected deployment.

Key changes:
- Direct read-only recovery from normalized legacy Supabase tables.
- Direct supplier recovery plus supplier derivation from product/PO/bill evidence.
- Projects recover into canonical jobs used by Project 360.
- Customers, Sales Orders, Purchase Orders, stock, allocations, movements, Goods Notes, bills and import history recover into current canonical collections.
- Automatic recovery is Admin-only, one-time and only for a genuinely empty canonical workspace.
- Partially populated workspaces remain dry-run/review/apply only.
- Recovery Diagnostics added to Settings.
- Global aqua table header/hover override removed.
- Sales Order, Purchase Order and Bundle table-header fallbacks neutralised.
- Project 360 neutral table styling explicitly protected.
- Network-first same-origin service-worker strategy prevents stale old CSS/JS from winning after deploy.

Verification:
- Production build: PASS.
- Legacy recovery/readability suite: PASS.
- End-to-end automatic legacy recovery into Projects/Customers/Suppliers/Sales: PASS.
- Project 360 visual/workspace: PASS.
- Sales Order, Purchase Order, Warehouse visual guards: PASS.
- Settings and Production Readiness checks: PASS.
- Contrast/readability checks: PASS.
- Public and dist runtime validation: PASS.
- 52/52 referenced release assets present and JS parses.
- Remaining non-database Accounting/Projects/Bundles/Dashboard checks: PASS.

Known test-environment boundaries:
- @electric-sql/pglite is declared but not installed in this extracted environment, so database-only test modules cannot start.
- Playwright is not installed, so automated browser smoke cannot start.
- No claim is made that those two dependency-bound suites passed.

The new release does not restore old UI or obsolete code. v1.22 keeps the approved rebuilt Pool Shed as the authority and only recovers useful business records into it.
